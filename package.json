{
  "name": "cyoa-engine",
  "version": "2.0.0",
  "description": "Choose Your Own Adventure story editor and engine",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "build:watch": "node esbuild.config.js --watch",
    "dev": "node esbuild.config.js --watch & npx serve dist",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "esbuild": "^0.20.0",
    "jsdom": "^27.4.0",
    "jszip": "^3.10.1",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "dependencies": {
    "cytoscape": "^3.28.0",
    "dompurify": "^3.0.6"
  }
}
